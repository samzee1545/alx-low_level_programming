#include "main.h"
/**
 * _islower - checks for lowercase characters
 * @arg1: First operand
 * Return: 0 (always)
 */
int _islower(int c)
{
	char sl;

	for (sl = 'a'; sl < 'z'; sl++)
	{
		if (c == sl)
		{
			return (1);
		}
	}
	return (0);
}
