#include <stdio.h>
/**
 * main - uses printf to print "with proper grammar,
 *                              but the outcome is a piece of art,"
 *
 * Return: 0 (always)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
